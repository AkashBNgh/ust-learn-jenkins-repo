pipeline {
    agent any 
    triggers cron ('H{
        cron ('H 4 * * 1-5')
    }
    stages {
        stage('build') {cron ('H
            steps {
                // Change 'sh' to 'bat' for a Windows environment
                bat 'node --version' 
            }
        }  triggers cron ('H {
            
        }
    }
}  triggers cron ('H {
    
}