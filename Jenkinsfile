pipeline {
    agent any 

    stages {
        stage('build') {
            steps {
                // Change 'sh' to 'bat' for a Windows environment
                bat 'node --version' 
            }
        }
    }
}